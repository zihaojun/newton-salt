elasticsearch-init:
   salt.state:
       - tgt: {{ salt['pillar.get']('basic:corosync:NODES') }}
       - tgt_type: list
       - sls:
         - dev.ha.elasticsearch
       - require:
         - salt: hosts-init

elasticsearch-service:
   salt.state:
       - tgt: {{ salt['pillar.get']('basic:corosync:NODES') }}
       - tgt_type: list
       - sls:
         - dev.ha.elasticsearch.service
       - require:
         - salt: elasticsearch-init

