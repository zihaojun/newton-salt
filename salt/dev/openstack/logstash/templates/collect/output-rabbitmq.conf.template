output{
  rabbitmq {
	host => "{{VIP}}"
	port => 5672 
	user => "{{ RABBIT_USER }}"
	password => "{{ RABBIT_PASS }}"
	exchange => "logstash-rabbitmq"
	exchange_type => "direct"
	durable => true 
	persistent => false
    }
}
